# PLDP-BO §4 smoke cell: dpfedavg_fixed — tiny T/K, assert off (§9.4/§9.5 smoke validation).
assert_locked_config: false
method: dpfedavg_fixed
seed: 0
data:
  name: mnist
  data_dir: ./data
  num_clients: 4
  partition_type: iid
  partition_min_samples: 30
  batch_size: 64
  val_split: 0.1
model:
  name: mlp
  num_classes: 10
federated:
  num_rounds: 20
  fraction_fit: 1.0
  fraction_evaluate: 0.0
  local_epochs: 1
  strategy: fedavg
  min_fit_clients: 4
  min_evaluate_clients: 4
  min_available_nodes: 4
  proximal_mu: 0.0
  server_learning_rate: 0.01
  aggregation: attenuation
optimizer:
  name: sgd
  lr: 0.01
  momentum: 0.9
  weight_decay: 0.0
  gradient_clip_norm: 0.0
privacy:
  enabled: true
  mechanism: gaussian
  clipping_mode: per_example
  update_clip_norm: 1.0
  accountant: rdp
  accountant_mode: rdp_native
  rdp_alpha: 10.0
  total_budget: 10.0
  enforce_budget: true
  fixed_rdp_target: 0.47619
personalization:
  enabled: false
bo:
  enabled: false
  optimization_metric: nun
  rdp_min: 0.01
  rdp_max: 2.0
  grid_points: 50
  acquisition_penalty: 0.1
  gp_kernel: matern52
  min_warmup: 10
  bounds_strategy: global
logging:
  tracker: mlflow
  run_name: smoke_dpfedavg_fixed